import { ScrollIntoViewDirective } from './scroll-into-view.directive';

describe('ScrollIntoViewDirective', () => {
	it('should create an instance', () => {
		const directive = new ScrollIntoViewDirective(null);
		expect(directive).toBeTruthy();
	});
});
