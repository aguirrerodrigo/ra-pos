import { SelectOnFocusDirective } from './select-on-focus.directive';

describe('SelectOnFocusDirective', () => {
	it('should create an instance', () => {
		const directive = new SelectOnFocusDirective(null);
		expect(directive).toBeTruthy();
	});
});
